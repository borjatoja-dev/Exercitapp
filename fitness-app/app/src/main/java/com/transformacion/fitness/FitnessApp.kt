package com.transformacion.fitness

import android.app.Application

/**
 * Application class - punto de entrada da aplicación.
 * Inicialización global (se necesaria) vai aquí.
 */
class FitnessApp : Application() {

    override fun onCreate() {
        super.onCreate()
        // Inicialización de componentes globais se é necesario
        // Por agora, deixamos baleiro para manter a simplicidade
    }
}
